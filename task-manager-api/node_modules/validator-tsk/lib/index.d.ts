import { Resources } from "resources-tsk";
import { IResult } from "result-tsk";
declare type ValidationType = Record<string, string | number | undefined | null | unknown | CallableFunction[]>;
declare type ErrorType = number | string;
export declare class Validator {
    private resources;
    private resourceKey;
    private defaultErrorCode;
    constructor(resources: Resources, resourceKey: string, defaultErrorCode?: ErrorType);
    isValidEntry(result: IResult, paramsToValidate: ValidationType, errorCode?: ErrorType): boolean;
}
export {};
